#! -a archive -r recurcive -z compress -P partial progress
# Test results
rcync  -av --dry-run source/* target/ # if you want to rcync entire dir and not just its content leave the '/*' at the end

# rcync to another machine use the user ip and dir as source or target:
user@ip:choosen_directory

# rcync compressed
rcync -zaP source/ target/

# only rsync last modified (update)
rcync -u
